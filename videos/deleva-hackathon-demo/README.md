# DELEVA cinematic demo

Separate HyperFrames project. Does not modify `web/` or `proof/`.

```bash
cd videos/deleva-hackathon-demo
npm run check
npm run dev          # Studio preview
npm run render       # after approval
```

Duration: 69.5s. Voice: local Kokoro `am_michael` (HeyGen not signed in).
